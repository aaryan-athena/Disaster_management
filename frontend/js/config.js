// Backend URL — points to the HuggingFace Spaces deployment.
// Update this value if your Space URL changes.
const BACKEND_URL = (window._BACKEND_URL || 'https://aaryan-athena-dm.hf.space').replace(/\/$/, '');
